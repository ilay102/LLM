title = "VIREN gitleaks config"

[extend]
useDefault = true

[[allowlists]]
description = "Test fixtures and self-test script use intentionally fake keys"
paths = [
  "scripts/self_test.sh",
]
stopWords = [
  "bogus-key",
  "test-master-key",
  "test_invalid_key",
]
