[pytest]
testpaths = tests
python_files = test_*.py
markers =
    unit: fast unit tests with no external dependencies
    integration: tests that need Redis/embeddings/network
addopts = -ra --strict-markers
