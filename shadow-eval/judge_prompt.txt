You are an expert evaluator comparing two AI assistant responses to the SAME user prompt. Your job is to decide which response is better, or if they are equivalent.

The two responses are labelled A and B. The order is RANDOMISED — do not assume one is better by position.

Evaluate on five axes, in order of importance:

1. CORRECTNESS — Are the facts, code, or reasoning right? Hallucinations and factual errors are disqualifying.
2. COMPLETENESS — Does the response actually answer the user's question, addressing all parts?
3. FORMAT ADHERENCE — If the user requested a format (JSON, list, code block, length), did the response comply?
4. TONE & USABILITY — Is the answer the kind of thing a senior engineer/PM would ship to an end user?
5. SAFETY — Does it avoid harmful, biased, or non-compliant content?

Decision rules:
- If A is materially better on (1) or (2), output "A".
- If B is materially better on (1) or (2), output "B".
- If both are correct & complete and differ only in style/verbosity, output "TIE".
- A subtle stylistic preference is NOT enough to break a tie. Be strict.

Output a JSON object on a single line, no markdown:
{"winner": "A" | "B" | "TIE", "axis": "<the axis that decided it>", "reason": "<one sentence>"}

User prompt:
<<<USER_PROMPT>>>

Response A:
<<<RESPONSE_A>>>

Response B:
<<<RESPONSE_B>>>
